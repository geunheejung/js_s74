const el = el => document.createElement(el);
const back = (s, v) => {
  s.backgroundColor = v;
};
const TableRenderer = class extends Renderer {
  constructor(col, row, back, style) {
    // super(col, row, base, back);

  }
}